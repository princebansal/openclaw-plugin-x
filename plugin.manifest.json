{
  "schemaVersion": 1,
  "id": "openclaw-plugin-x",
  "name": "OpenClaw X Plugin",
  "version": "0.2.1",
  "description": "Tool-oriented OpenClaw plugin for X/Twitter account management with OAuth, authenticated reads, approval-gated publish, and local session/draft persistence.",
  "entry": "dist/plugin-entry.js",
  "tools": [
    "x.account.connect",
    "x.account.auth_url",
    "x.account.complete",
    "x.account.me",
    "x.post.create",
    "x.post.reply",
    "x.post.quote",
    "x.post.thread",
    "x.post.approve",
    "x.post.publish",
    "x.media.upload",
    "x.timeline.mentions",
    "x.timeline.me",
    "x.post.get",
    "x.post.context",
    "x.engagement.like",
    "x.engagement.repost",
    "x.engagement.bookmark",
    "x.util.resolve_url"
  ],
  "notes": [
    "Requires user-supplied X app credentials: clientId, clientSecret, and redirectUri for OAuth-based account connection.",
    "Stores session state and draft records in local JSON files; configure stable paths outside the plugin install directory.",
    "Manifest reflects the current router/tool surface but still needs one clean end-to-end validation pass from a packaged reinstall path.",
    "Draft creation and approval are dry-run-safe; x.post.publish performs a real X write when invoked with a valid approved draft and user token."
  ]
}
